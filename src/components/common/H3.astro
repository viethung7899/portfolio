---
import Icon from "astro-icon";
const slug = Astro.props.id;
---
<h3 class="group" id={slug}>
  <slot />
  <a class="hidden group-hover:inline-block hover:text-orange-500 hover:dark:text-teal-500" href={`#${slug}`}>
  <Icon name="fa-solid:link" class="w-4 h-4" />
</a></h3>